package main.java.model;

public class GameHand {
    // just pseudocode
    /*public void distribuiteCard(){
         for (Player pl : playersInGame){
             PlayerHand ph = pl.getHand();
             List<Card> deck = Deck.getDeck();
             ph.getHand.add(Deck.removeTopCard());//first card
             ph.getHand.add(Deck.removeTopCard());//second card
         }
     }*/
}
